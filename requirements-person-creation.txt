torch
torchvision
langgraph
opencv-python
ultralytics
huggingface_hub
facenet-pytorch
transformers==4.55.4
accelerate
pyyaml
flask
flask-cors
sentencepiece
einops
timm
scipy

# Optional — auto_pair association cues. Each is probed at load_models time;
# if missing, that cue is disabled and its weight is redistributed across the
# remaining cues. Install to enable body-ReID / pose cues:
#   torchreid   (OSNet-x0.25 body ReID embedder)
#   rtmlib      (RTMPose keypoints, onnxruntime-only, no mmcv/mmdet needed)

# Optional — GPU-first video pipeline (PERSON_CREATION_GPU_PIPELINE=1).
# NVDEC hardware decode; capability-probed at runtime with a logged CPU
# fallback, so the base pipeline works without it:
#   PyNvVideoCodec  (NVIDIA's official pip wheel; needs libnvcuvid — present
#                    in WSL2 with driver >= 545 and any native Linux driver)
