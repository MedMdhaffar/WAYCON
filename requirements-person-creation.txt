torch
torchvision
langgraph
opencv-python
ultralytics
huggingface_hub
facenet-pytorch
transformers==4.55.4
accelerate
pyyaml
flask
flask-cors
sentencepiece
einops
timm
scipy
numpy

# GStreamer bindings (PyGObject/gi) for realtime RTSP ingestion
# (forensics/person_creation/gst_stream.py). PyGObject itself is a pip package,
# but it links against system GObject-introspection + GStreamer libraries that
# pip cannot install -- see the apt packages listed in gst_stream.py's module
# docstring / the realtime-ingestion runbook. Without those system packages
# `import gi` / `gi.require_version("Gst", "1.0")` will fail.
PyGObject>=3.50

# Optional — auto_pair association cues. Each is probed at load_models time;
# if missing, that cue is disabled and its weight is redistributed across the
# remaining cues. Install to enable body-ReID / pose cues:
#   torchreid   (OSNet-x0.25 body ReID embedder)
#   rtmlib      (RTMPose keypoints, onnxruntime-only, no mmcv/mmdet needed)
